pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
    }
}

rootProject.name = "Native Application"
include ':app'
