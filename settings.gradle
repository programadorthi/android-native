pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral() // God, Java androidx modules still needing Kotlin :(
    }
}

rootProject.name = "Native Application"
include ':app'
